// Given data
const costPerUnit = 2.50; // Cost per unit of "Energon" in Rands
const stockLevel = 30;    // Stock level of "Energon" units

// Calculate total value without discount
const totalValueWithoutDiscount = ;

// Apply discount if stock level is more than 20 units
let ;

// Check if stock level is more than 20 units
if (stockLevel > 20) {
    // Apply 10% discount
    const discount = ; // Calculate the discount amount (10% of cost per unit)
    totalValueWithDiscount = ; // Calculate total value with discount
}

// Output results
console.log(`Total value without discount: R${}`); // Display total value without discount
if (totalValueWithDiscount !== ) {
    ; // Display total value with discount (if applicable)
}
